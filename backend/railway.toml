[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements-local.txt"
nixpacksConfigPath = "nixpacks.toml"

[deploy]
startCommand = "sh -c 'exec uvicorn server:app --host 0.0.0.0 --port ${PORT}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
